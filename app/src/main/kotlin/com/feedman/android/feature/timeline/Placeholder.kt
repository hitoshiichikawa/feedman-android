/**
 * Package marker for `feature/timeline` (Req 2.7).
 *
 * `TimelineScreen.kt` and `TimelineViewModel.kt` provide the skeleton's mock-mode
 * timeline. Cross-feed Paging 3 integration is deferred to a subsequent Issue.
 */
package com.feedman.android.feature.timeline
