/**
 * Package marker for `feature/login` (Req 2.7).
 *
 * The login placeholder Composable lives in `LoginPlaceholderScreen.kt`. The real
 * Google OAuth UI (Custom Tabs + PKCE) is deferred to a subsequent Issue per
 * `docs/GRAND-DESIGN.md` §5.3.
 */
package com.feedman.android.feature.login
