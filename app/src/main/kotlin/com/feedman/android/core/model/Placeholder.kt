/**
 * Package placeholder for `core/model` (Req 2.7).
 *
 * Real model implementations (`CrossFeedItem`, `Subscription`, `User`, `Page`) will be
 * introduced by subsequent Issues per `docs/GRAND-DESIGN.md` §3.
 */
package com.feedman.android.core.model
