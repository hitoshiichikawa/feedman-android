/**
 * Package placeholder for `core/network` (Req 2.7).
 *
 * Retrofit-based `FeedmanApi`, `AuthInterceptor`, `TokenAuthenticator`, and the cursor
 * pagination scaffolding (`paging/`) will be introduced by subsequent Issues per
 * `docs/GRAND-DESIGN.md` §3 / §5.1 / §5.2 / §5.3.
 */
package com.feedman.android.core.network
