# Gradle JVM options
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX
android.useAndroidX=true

# Kotlin
kotlin.code.style=official

# Skeleton: do not enable mockMode / override baseUrl by default.
# Override via `-Pfeedman.mockMode=true` / `-Pfeedman.baseUrl=https://...` or by uncommenting below.
# feedman.mockMode=false
# feedman.baseUrl=https://dev.feedman.example.com
